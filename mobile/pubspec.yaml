name: kavasam_mobile
description: "An offline-first Android caller ID and spam protection phone app."
publish_to: "none"
version: 4.0.0+7

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
